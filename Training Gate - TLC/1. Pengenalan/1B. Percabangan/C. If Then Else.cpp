#include<bits/stdc++.h>
using namespace std;
int num;

int main()
{
	cin>>num;
	if (num>0) cout<<"positif"<<"\n"; else
	if (num==0) cout<<"nol"<<"\n"; else
	if (num<0) cout<<"negatif"<<"\n";
return 0;	
}

